# v3.3.2 H0-H5 生产候选硬化收束记录

日期：2026-05-17

本记录归档 P0-P6 之后的生产候选硬化收束。目标不是继续扩展业务功能，而是把固定 runner、长稳、容量、Kubernetes、观测和 SDK 企业接入证据统一到可检查的交付面。

## 交付结论

H0-H5 已完成可执行证据收束：

| 阶段 | 状态 | 证据 |
| --- | --- | --- |
| H0 固定 Runner 常态化 | completed | `production-resilience.yml` / `production-evidence.yml` weekly schedule、runner fallback、artifact 归档 |
| H1 长稳与资源曲线 | completed for executable evidence | `verify_stability_soak.py`、`nightly-stability.yml`、P5/P6 聚合入口 |
| H2 容量边界与退化阈值 | completed for gate contract | `collect_release_baseline.py`、capacity preset、release gates |
| H3 Kubernetes 发布演练 | completed for static/kind entry | K8s resources/HPA/PDB、`operator_kind_smoke.py`、kind gate |
| H4 观测闭环增强 | completed for runtime entry | `verify_observability_gate.py --include-runtime-http --include-otel-collector`、Grafana RED panels |
| H5 SDK 企业接入包 | completed | Python/C# full-flow examples、SDK heartbeat/disconnect/version diagnostics docs、SDK/Gateway compatibility matrix |

## 验证入口

```bash
python3 scripts/check_production_hardening_gate.py \
  --summary-path runtime/validation/h0-h5-hardening-summary.json
```

通过标准：

- `runtime/validation/h0-h5-hardening-summary.json` 中 `passed=true`。
- H0-H5 每个阶段至少有 workflow、脚本、manifest、文档或示例证据。

## 边界

- 2h/8h soak、5K/10K 多轮容量数据和真实云 Kubernetes rollout/rollback 仍需要固定 runner 持续沉淀。
- 本轮不伪造长跑数据，只固定入口、归档契约和失败归因。
