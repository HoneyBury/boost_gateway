# v3.5.2 Freeze TODO

更新时间：2026-07-17

本文档是 `v3.5.2` 剩余冻结工作的执行清单。版本范围和延期决策以
`docs/v3.5.x-maintenance-plan.md` 为准，已实现能力以 `docs/current-state.md` 为准，
runner 准入规则以 `docs/runner-gate-standard.md` 为准。任何成功结论都必须记录完整
commit SHA、workflow run、runner、artifact 和 summary，不能跨 SHA 拼接。

## 当前基线

- [x] AOI Release run `29564215641`：完整 build/test/gates、clean Ubuntu runtime consumer 和 SPDX SBOM 通过。
- [x] AOI Operator kind run `29563770679`：Ready、scale、rollout restart/undo、Operator restart、delete 和 cleanup 通过。
- [x] AOI 增强 R0 run `29564768686`：Redis live、runtime HTTP、release baseline、P5/P6 kind 和 N5 通过。
- [x] Python/C# 冻结为源码 wrapper/example；`v3.5.x` 不承诺 wheel/NuGet 分发。
- [x] 独立 debug-symbol 包延期到下一次 minor。
- [ ] compiler-bearing clean container 中的 CMake SDK consumer。
- [ ] 第二台 Linux runner 准入和同资产复验。
- [ ] 最终冻结 SHA 上的 Release/R0 同 SHA 刷新。
- [ ] `v3.5.2` tag publish、attestation 和线上资产独立验收。

## 第二台 Runner：操作者清单

以下 `<runner-name>`、`<runner-id>` 和 `<unique-label>` 必须替换为实际值。不要在日志、
文档或 issue 中保存 registration token、GitHub token、registry credential 或内网地址。

### A. 恢复和身份确认

- [ ] 启动 runner service，确认 GitHub API 显示 `online`、`busy=false`。
- [ ] 确认系统 labels 包含 `self-hosted`、`Linux`、`X64`。
- [ ] 确认存在唯一机器标签，推荐 `node-<runner-name>`。
- [ ] 记录 runner ID、名称、Ubuntu release、kernel、CPU、内存、swap 和根分区可用空间。
- [ ] 不要提前添加共享 `preprod-r5` 标签。

只读核验入口：

```bash
gh api repos/HoneyBury/boost_gateway/actions/runners \
  --jq '.runners[] | {id,name,status,busy,labels:[.labels[].name]}'

uname -a
cat /etc/os-release
gcc --version
python3 --version
docker version
docker compose version
df -h /
free -h
```

通过条件：Ubuntu x64、Docker/Compose 可用、构建/import 前至少 25GB 可用空间；
8GiB/no-swap 机器的 C++ 构建并行度不得超过 2。

### B. G2 Conan 严格离线准入

- [ ] `/opt/boost-gateway/{conan,sccache,tools}` 对 runner 用户可写。
- [ ] 使用 Python 3.12 + Conan `2.8.1` 隔离 venv，不使用系统/全局 Conan。
- [ ] 从 `release/3.5` 的候选 checkout 解析 runner 专属 namespace。
- [ ] 如 namespace 缺包，只在显式预热阶段使用批准的 remote/mirror。
- [ ] 关闭 remote 后执行 `--no-remote --build=never`，确认全部 lockfile 包命中。
- [ ] 保存 `runner-cache-identity.json`、Conan 版本和离线 install 日志。

关键验收命令：

```bash
conan_venv=/opt/boost-gateway/tools/conan-2.8.1-py3.12
python3.12 scripts/tools/ensure_conan_venv.py \
  --venv "$conan_venv" --conan-version 2.8.1 --offline
export PATH="$conan_venv/bin:$PATH"

cache_env="$(mktemp)"
python3 scripts/tools/resolve_runner_cache.py \
  --build-type Release \
  --profile conan/profiles/linux-gcc-x64 \
  --lockfile conan/locks/linux-gcc-x64-release-nogrpc-nosqlite.lock \
  --github-env "$cache_env" \
  --summary-path runtime/validation/runner-cache-identity.json
set -a; . "$cache_env"; set +a

python3 scripts/bootstrap_conan.py --conan-home "$CONAN_HOME" --no-remote
conan install . \
  --profile:host conan/profiles/linux-gcc-x64 \
  --profile:build conan/profiles/linux-gcc-x64 \
  --lockfile conan/locks/linux-gcc-x64-release-nogrpc-nosqlite.lock \
  -o "&:with_grpc=False" -o "&:with_sqlite=False" \
  --output-folder=build/conan-second-runner-acceptance \
  --build=never --no-remote -s build_type=Release
```

如果 `--offline` 检查或最终 install 失败，保持 runner 未准入；不得在证据 job 内临时
启用公网 remote 或 `--build=missing`。

### C. Docker 和发行包最低准入

- [ ] Docker daemon 中存在 `ubuntu:24.04`；正式验证使用 `--pull=never`。
- [ ] 在第二 runner 的唯一标签上手动执行 Release workflow。
- [ ] 确认 workflow checkout SHA 等于选定候选 SHA。
- [ ] 确认 archive layout、clean Ubuntu consumer 和 SPDX SBOM 步骤通过。
- [ ] 下载 artifact，独立检查 tarball、SBOM 和 consumer summary。

候选分支必须先固定到待验证 SHA，再执行：

```bash
gh workflow run release.yml \
  --repo HoneyBury/boost_gateway \
  --ref release/3.5 \
  -f 'runner=["self-hosted","Linux","X64","<unique-label>"]' \
  -f conan_lockfile=conan/locks/linux-gcc-x64-release-nogrpc-nosqlite.lock \
  -f perf_preset=smoke \
  -f perf_repetitions=3
```

最低冻结证据是该 Release run 和同一线上资产的独立下载验证。它不会自动赋予 runner
`preprod-r5` 能力。

### D. 可选的完整 G3-G5 / R5 准入

仅当第二 runner 后续要加入 `preprod-r5` 能力池时执行：

- [ ] 当前 Compose 所需全部镜像已按 `linux/amd64` 预热或从可信 bundle 导入。
- [ ] `docker_pull_policy=never` image preflight 为 0 missing。
- [ ] G0-G3 通过后，按准入标准追加唯一 R5 label 和共享 `preprod-r5` label。
- [ ] 使用机器唯一 label 执行 R5 gateway restart、前后 SDK full-flow 和 snapshot。
- [ ] R6 两轮通过，R5/R6 artifact 与候选 SHA 一致。
- [ ] G4-G5 通过后，才允许 R2/R3 将该 runner 的结果作为生产候选证据。

完整命令和 bundle 传输方式见 `docs/fixed-runner-playbook.md`。任一 G2/G3 漂移后先移除
共享能力标签，再重新准入。

## 第二台 Runner：正式资产复验

在 `v3.5.2` tag publish 成功后，于第二 runner 的 `v3.5.2` detached checkout 根目录
执行。资产必须下载到新建的空目录，验证脚本必须来自同一 tag：

```bash
git fetch origin tag v3.5.2
git switch --detach v3.5.2

asset_dir=runtime/validation/v3.5.2-release-assets
mkdir -p "$asset_dir"
test -z "$(find "$asset_dir" -mindepth 1 -maxdepth 1 -print -quit)"

gh release download v3.5.2 \
  --repo HoneyBury/boost_gateway \
  --dir "$asset_dir" \
  --pattern 'boost-gateway-v3.5.2-linux-x64.tar.gz' \
  --pattern 'boost-gateway-v3.5.2-sbom.spdx.json' \
  --pattern 'SHA256SUMS.txt'

(cd "$asset_dir" && sha256sum -c SHA256SUMS.txt)

python3 scripts/tools/verify_release_archive.py \
  --archive "$asset_dir/boost-gateway-v3.5.2-linux-x64.tar.gz" \
  --expected-root boost-gateway-v3.5.2-linux-x64

python3 scripts/tools/verify_release_package_consumer.py \
  --archive "$asset_dir/boost-gateway-v3.5.2-linux-x64.tar.gz" \
  --expected-root boost-gateway-v3.5.2-linux-x64 \
  --image ubuntu:24.04 \
  --summary-path runtime/validation/second-runner-package-consumer-summary.json

gh attestation verify "$asset_dir/boost-gateway-v3.5.2-linux-x64.tar.gz" \
  --repo HoneyBury/boost_gateway
```

通过条件：checksum 全通过；tarball 为真实 gzip 且只有单一版本根目录；全部必需 ELF
依赖可解析；hello-world 在 `network=none` / `pull=never` 的 Ubuntu 容器内退出码为 0；
GitHub build provenance 和 SBOM attestation 可验证。

## 项目侧剩余工作

### Clean CMake Consumer

- [ ] 准备版本固定、已准入且本地缓存的 Linux compiler/CMake/Ninja 容器镜像。
- [ ] 运行时使用 `--network=none --pull=never`。
- [ ] 从发行 tarball 解压后的 CMake package 执行 `find_package()`、configure、build、run。
- [ ] summary 记录容器 image ID、编译器/CMake 版本、tarball SHA-256 和候选 SHA。
- [ ] 不使用 AOI host consumer + runtime container 两份证据拼接冒充本项。

### 最终候选和 Tag

- [ ] 冻结一个不再移动的完整 SHA，并确认 `release/3.5` 指向该提交。
- [ ] 同一 SHA 上通过本地治理门禁、Release workflow 和增强 R0。
- [ ] 核验 R0 provenance 的 candidate、checkout、runner、configuration、lockfile 全部匹配。
- [ ] 创建不可移动的 annotated `v3.5.2` tag。
- [ ] tag workflow 的 package、publish、build provenance attestation 和 SBOM attestation 通过。
- [ ] GitHub Release 包含 tarball、SPDX SBOM、`SHA256SUMS.txt` 和完整 CHANGELOG 正文。
- [ ] AOI 和第二 runner 分别独立下载并验收同一线上资产。
- [ ] 回填 tag SHA、run ID、artifact/asset ID、tarball/SBOM SHA-256 和 attestation URL。
- [ ] 更新 GitHub issue #3；所有必需项完成后关闭 issue 并将 `v3.5.2` 标记为已发布。

本轮没有运行时代码、默认协议或部署配置变化；只要后续不引入这些变化，不要求重新
执行 2h、R5/R6 和最终 R2/R3。若冻结前出现任何运行时代码或默认行为变化，本豁免失效，
必须重新评估并刷新对应高成本证据。

## 证据回填模板

| 字段 | 值 |
|---|---|
| Freeze commit SHA | 待填写 |
| Second runner name / unique label | 待填写 |
| Runner OS / GCC / Docker / Conan | 待填写 |
| Runner cache identity artifact | 待填写 |
| Second-runner Release run / artifact | 待填写 |
| Clean CMake consumer run / artifact | 待填写 |
| Enhanced R0 run / artifact | 待填写 |
| Tag publish run | 待填写 |
| Tarball SHA-256 | 待填写 |
| SBOM SHA-256 | 待填写 |
| Build provenance attestation | 待填写 |
| SBOM attestation | 待填写 |
| Independent asset verification | 待填写 |
