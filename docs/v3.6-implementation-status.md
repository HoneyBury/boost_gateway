# v3.6 P0-P6 Implementation Status

更新时间：2026-07-22

P0-P6 的仓库内实现已完成，默认激活策略仍是
`blocked_until_gates_pass`。本页区分本地可复现事实与必须由固定 runner 或不可变
发布资产补齐的外部事实；后者未完成前不得宣称 production supported。

| 阶段 | 仓库内结果 | 已有验证 | 尚需外部证据 |
|---|---|---|---|
| P0 Raft schema A/B | strict versioned disk codec、legacy migration/history、protobuf schema、dual reader、capability 与真实 mixed-binary gate | 本地全量基线、三节点升级/回滚；Linux Release `29912054968` 的真实 mixed-binary gate | 包含后续 workflow 修复的最终冻结 SHA refresh |
| P1 Raft command codec | matchmaking/leaderboard typed command、strict legacy/protobuf reader、确定性 fixture | command codec、service focused CTest 与 Linux Release `29912054968` | 与 P2 一起进入最终 frozen-SHA 发布证据 |
| P2 Phase C writer/apply | 显式配置 + 全 peer capability 才写 protobuf；能力撤销回落 legacy；apply 成功后才推进并持久化 index，失败 fail closed/restart retry | writer/capability/apply failure/restart 回归 | candidate 集群同 SHA rolling/rollback run |
| P3 Identity/JWKS | static multi-kid 与 JWKS resolver、immutable snapshot、TTL/stale grace、单 worker refresh、严格 RS256/JWK/config 门禁 | identity/security focused tests、security release gate；Linux x64 exact-checkout HTTPS drill 10/10、probe 16/16、contract 6/6 | macOS ARM64 exact-SHA JWKS 与最终跨平台冻结集 |
| P4 macOS ARM64 | Conan profile/lockfile/preset、原生 install verifier、候选 workflow | 本机 ARM64 Mach-O/C ABI/CMake consumer package gate | macOS ARM64 workflow 与不可变资产独立复验 |
| P5 SDK distribution | SDK 4.2.0 platform wheel/NuGet builder、包内 native manifest、clean consumer verifier、真实 package full-flow、候选 workflow | macOS wheel 13/13；Linux x64 dedicated workflow package 25/25、fresh no-index flow 15/15、逐包 SPDX/checksum | osx-arm64 SDK、正式线上资产与发布后独立消费 |
| P6 Debug symbols | Linux ELF split/strip/debuglink/build-id 映射、独立 archive verifier、受控崩溃探针、runbook/workflow | Linux x64 dedicated workflow 14-ELF pair 115/115；probe 11/11；逐资产 SPDX/checksum | 性能对照、正式发布双资产与发布后独立复验 |

## 2026-07-22 Linux x64 进展

线上 exact-SHA 批次固定在 `d687b9ed4293e6ea300ddc14d5868f59d989795c`：Release
run `29912054968`、R0 run `29912422049`、R5/R6 run `29913176854` 均在 AOI
Linux x64 runner 上通过并携带一致 checkout/lockfile provenance。Release 包含真实
`v3.5.3` mixed-binary Raft 边界；R0 覆盖 Redis、kind、runtime HTTP、P5/P6 和 SDK；
R5/R6 覆盖 11-image 离线准入、真实恢复、告警状态迁移与两轮 TLS。

默认分支经 PR #12-#16 注册并修复了三条专用路径。最终 workflow 候选
`00ce82e0b00d57f1ca68558d1e31fefd09fe506d` 在同一 AOI Linux x64 runner 和同一
strict-offline lockfile graph 上完成：debug-symbol run `29922341090`、artifact
`8530608986`，14 ELF 独立验证 115/115、受控崩溃探针 11/11；SDK run
`29923068133`、artifact `8530788482`，wheel/NuGet 25/25、fresh no-index flow
15/15；JWKS run `29923314097`、artifact `8530992597`，外层 10/10、C++ probe
16/16、summary contract 6/6。所有 summary 的 candidate/run/runner provenance
一致，SDK 与 debug-symbol checksum 已下载复验；debug artifact 只保留 11 个声明
文件，不再上传未校验的物化工作目录。

这组 dedicated artifact 关闭 Linux x64 对应的 workflow 注册、offline package、JWKS
和 symbol 边界，但不把较早 `d687b9e` 的 Release/R0/R5-R6 或 ARM/macOS 结果改绑到
`00ce82e0`。持久证据归档位于
`/home/aoi/actions-runner/_evidence/boost-gateway/v3.6/aoi-runner-20260722`，当前
`MANIFEST.sha256` 覆盖并通过 686/686 文件复验。

## 激活顺序

1. Linux `sdk-distribution.yml`、`debug-symbols.yml` 与 `jwks-rotation.yml` 已在
   `00ce82e0` 保存 summary、artifact ID、runner identity 与 lockfile hash；最终跨平台
   冻结时仍需把 `release.yml` 及其它平台刷新到统一候选 SHA。
2. 运行 `macos-arm64.yml`，只在同 SHA build/test/install/package 全通过后把
   `osx-arm64` 从 local candidate 提升为 verified candidate。
3. Linux fixed-runner JWKS 已完成；macOS ARM64 仍需保存对应真实 HTTPS 轮换、
   stale grace、outage、静态 key-ring 回滚 summary 和 artifact ID。
4. 冻结候选 tag 后发布 GitHub Release 资产，再由独立 workflow 下载并核对
   checksum、SBOM、provenance、包消费和符号映射。
5. 只有具体版本/RID 的全部证据闭环后才更新 compatibility matrix；PyPI、
   NuGet.org、Raft protobuf writer 默认启用仍需独立激活决策。
