# v3.3.2 P1 性能稳定化记录

日期：2026-05-17

提交基线：`63668b0e863f8b52996937c2ab46ddbc6d02bb74`

本记录用于归档 P0/P1 性能事实管线和低风险优化实验。当前阶段不扩展业务功能，优先保证 baseline 可解释、可重复、低波动。

## 交付结论

P1 默认稳定线保持 `backend_pool_size=1`。该配置下 Release baseline 单轮通过，且所有 case 的 rejected/failed/forced_timeout 均为 0。

多 backend connection pool 仍作为实验项保留在采集参数中，不进入默认 release baseline。显式 `backend_pool_size=8` 的 smoke 实验在 battle 场景触发 backend_error/rejected，说明当前 backend connection 生命周期、熔断和多连接池交互仍会放大波动。

## 默认 baseline 结果

报告路径：

- `runtime/perf/p1-default-pool-baseline-check/report.md`
- `runtime/perf/p1-default-pool-baseline-check/summary.json`

命令：

```bash
python3 scripts/collect_v2_perf_baseline.py \
  --build-dir build/release \
  --run-preset baseline \
  --repetitions 1 \
  --output-root runtime/perf/p1-default-pool-baseline-check
```

| Case | p99 | 吞吐 | rejected | failed | forced_timeout |
| --- | ---: | ---: | ---: | ---: | --- |
| echo-100-30s | 5ms | 1861/s | 0 | 0 | false |
| echo-1000-30s | 20ms | 18255/s | 0 | 0 | false |
| battle-20-30s | 20ms | 542/s | 0 | 0 | false |
| battle-100-30s | 200ms | 1583/s | 0 | 0 | false |

Gateway 资源样本：

| Case | RSS delta | RSS KB/client | fd delta |
| --- | ---: | ---: | ---: |
| echo-1000-30s | 16.33MB | 16.722 | 4 |
| battle-100-30s | 19.16MB | 196.198 | 12 |

## 实验风险记录

报告路径：

- `runtime/perf/p1-backend-pool8-smoke-experiment/report.md`
- `runtime/perf/p1-backend-pool8-smoke-experiment/summary.json`

命令：

```bash
python3 scripts/collect_v2_perf_baseline.py \
  --build-dir build/release \
  --run-preset smoke \
  --repetitions 1 \
  --backend-pool-size 8 \
  --output-root runtime/perf/p1-backend-pool8-smoke-experiment
```

结果：

| Case | 结果 | 说明 |
| --- | --- | --- |
| echo-20-10s | passed | p99 2ms，rejected/failed/forced_timeout 为 0 |
| battle-2-10s | failed | rejected=2，body=backend_error |

结论：`backend_pool_size>1` 不作为当前默认优化项。后续若继续推进，需要先设计 backend connection 生命周期、连接健康检查、熔断粒度和连接池回收策略，再进入性能优化。

## P1 代码与报告能力

- `scripts/collect_v2_perf_baseline.py`：生成 `resource_analysis` 和 `report.md`，并记录 `backend_connection_pool_size`。
- `scripts/collect_release_baseline.py`：release summary 记录 `performance_summary_path` 与 `performance_report_path`。
- `docs/performance-baseline.md`：补充报告结构、资源字段和 P1 实验口径。

## 后续约束

- 默认 release baseline 必须保持 `backend_pool_size=1`，除非新的专项报告证明更大连接池稳定。
- `battle-100` p99 当前仍为 200ms，虽然通过 250ms gate，但接近高并发 battle 的重点观察区。
- 后续优化必须带同机同命令对比报告，避免凭单次 smoke 结论改变默认路径。

## P1 生产稳定化刷新

提交基线：`0afadc6`

本轮 P1 面向生产稳定化阶段重新刷新 Release baseline，并修复两个性能事实风险：

- `scripts/collect_v2_perf_baseline.py` 在多构建目录下会优先选择当前 build dir 的直接产物，避免误捡 `build/build` 等历史嵌套产物。
- 指定 `--output-root` 时会清理旧 `logs/`、`results/`、`summary.json` 和 `report.md`，避免 smoke 或失败产物混入正式 baseline 目录。

### Release baseline 三轮

报告路径：

- `runtime/perf/release-baseline/report.md`
- `runtime/perf/release-baseline/summary.json`
- `runtime/validation/p1-release-baseline-summary.json`

命令：

```bash
python3 scripts/collect_release_baseline.py \
  --build-dir build/release \
  --configuration Release \
  --skip-build \
  --skip-r4 \
  --perf-preset baseline \
  --perf-repetitions 3 \
  --perf-timeout-seconds 900 \
  --summary-path runtime/validation/p1-release-baseline-summary.json
```

结果：`release_gates.overall_pass=true`，warnings=0。

| Case | connected | messages | throughput | P50 | P90 | P99 | rejected | failed |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| echo-100-30s | 100 | 55,298 | 1,836.23/s | 5ms | 5ms | 10ms | 0 | 0 |
| echo-1000-30s | 1,000 | 535,536 | 17,802.35/s | 5ms | 5ms | 20ms | 0 | 0 |
| battle-20-30s | 20 | 8,855 | 542.91/s | 5ms | 10ms | 20ms | 0 | 0 |
| battle-100-30s | 100 | 39,876 | 1,812.63/s | 50ms | 100ms | 200ms | 0 | 0 |

Gateway 资源聚合：

| Case | RSS | RSS delta | RSS KB/client | fd | fd delta | CPU snapshot |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| echo-1000-30s | 20.42MB | 16.48MB | 16.876 | 37 | 16 | 21.8% |
| battle-100-30s | 21.20MB | 17.26MB | 176.742 | 66 | 45 | 6.1% |

### Capacity 专项

报告路径：

- `runtime/perf/p1-capacity-local/report.md`
- `runtime/perf/p1-capacity-local/summary.json`

命令：

```bash
python3 scripts/collect_v2_perf_baseline.py \
  --build-dir build/release \
  --run-preset capacity \
  --repetitions 1 \
  --output-root runtime/perf/p1-capacity-local
```

结果：`release_gates.overall_pass=false`。这是容量退化点记录，不作为 release baseline pass 条件。

| Case | connected | throughput | P99 | rejected | failed | 结论 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| echo-1000-30s | 1,000 | 18,547.27/s | 10ms | 0 | 0 | pass |
| echo-5000-30s | 1,347 | 23,952.28/s | 20ms | 0 | 3,653 | 连接建立退化 |
| echo-10000-30s | 1,299 | 23,072.81/s | 20ms | 0 | 8,701 | 连接建立退化 |
| battle-100-30s | 100 | 1,745.15/s | 200ms | 0 | 0 | pass |
| battle-500-30s | 361 | 1,158.37/s | 500ms | 139 | 0 | battle 高并发退化 |

当前 P1 结论：

- `baseline` 可作为生产候选 Release 性能门槛，当前三轮通过。
- `capacity` 已明确当前单机退化点：5K/10K echo 主要卡在连接建立失败，battle-500 卡在 rejected 与 P99 500ms。
- 下一轮性能优化不应先改业务功能，应优先定位连接建立上限、OS backlog/fd、accept 分布、压测客户端并发模型和 battle fan-out/ready/start 调度。
