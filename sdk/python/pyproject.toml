[build-system]
requires = ["setuptools>=61.0", "wheel>=0.43"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "boost-gateway-sdk"
version = "4.2.0"
description = "Boost Gateway Game Server SDK"
requires-python = ">=3.8"
